using System;

namespace Test
{
    class Program 
    {
        float user_inprint;      // (данные от пользователя)
        {
            user_inprint = float.Parse(Console.ReadLine());
            
            
        }

        // user_inprint = Convert.ToDouble(Console.ReadLine()); // (конвертация данных от пользователя в тип данных double)
        // Если в конвертации отсутствует тип данных к оторый хоть конвертировать, то конвертация невозможно
        // Например ToFloat отсутствует.




    }
}